// import ym, { YMInitializer } from 'react-yandex-metrika';

// export const getMetrikaCounter = async (counterId: number) => {
//   const yaMetrika = await (window as any).ym;
//   const counter = await yaMetrika.get(counterId);
//   return counter;
// };